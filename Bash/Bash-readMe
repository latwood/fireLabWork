The idea of all the bash scripts and files here is that they have a short explanation in the headers of the files instead of using a readMe. The reason is that each file is completely separate from any other file, which is not the same for many of the scripts and applications written in other languages.

